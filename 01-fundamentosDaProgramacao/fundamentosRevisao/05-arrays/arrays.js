// Arrays
// Aceita vários e todos os tipos de dados em uma única variável
// Nos permite criar um conjuntos de dados ordenados.
// Tem índices começando por 0
// Para achar o tamanho do array usando lengTH


let pessoa = ['Gaspar', 37, 1.78, true, ['Rock', 'corrida']]

console.log(pessoa[0])
console.log(pessoa[4])
console.log(pessoa.length)